#lang scheme

(require "geometry.ss")

;; Various effects on shapes.

;; quad->grid : quad number -> (streamof quad)
(define (quad->grid quad step)
  (void))

;; quad->strip : quad number -> (streamof quad)
(define (quad->string quad number)
  (void))     